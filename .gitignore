# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/

# macOS
.DS_Store

# Swift
.swiftpm/
Package.resolved

# Libretro binary'leri (lisans nedeniyle kendin indireceksin)
Resources/Cores/*.dylib

# make-core-frameworks.sh ile dylib'lerden üretilen .framework sarmalayıcıları
# (kaynak dylib'ler zaten ignore'lu; bunlar da üretilen artefakt)
CoreFrameworks/

# App Store export config (lokal; istersen commit'leyebilirsin)
ExportOptions.plist

# ROM/BIOS (asla commit'lenmez)
*.iso
*.bin
*.cue
*.z64
*.gcm
*.cso
*.pbp

# IDE
.vscode/
.idea/
